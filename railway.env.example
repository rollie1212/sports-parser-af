# Railway Environment Variables
# Copy these to Railway Dashboard → Variables

# MongoDB connection string (MongoDB Atlas recommended)
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/football-bot

# API-Football key (get from https://api-sports.io/)
API_KEY=your_api_football_key_here

# Update interval in minutes (1 = every minute)
FETCH_INTERVAL_MINUTES=1

# Number of days ahead to fetch fixtures (5 = today + 5 days)
DAYS_AHEAD=5

# Note: PORT is automatically set by Railway
